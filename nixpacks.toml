[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r backend/requirements.txt"
]

[start]
cmd = "PYTHONPATH=backend python -m uvicorn app.main:app --host 0.0.0.0 --port 8080"
